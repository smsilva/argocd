source:
  repo: https://github.com/smsilva/argocd.git
  targetRevision: wasp
  path: charts

destination:
  cluster: in-cluster

applications:
  - name: terraform-controller
    namespace: default
    wave: 0

  - name: wasp-base
    namespace: default
    wave: 0

  - name: external-secrets
    namespace: default
    wave: 0

  # Crossplane Composition + terraform-controller + StackInstance Object
  # - name: wasp-clusters
  #   namespace: default

  # terraform-controller + StackInstance Object
  # - name: azure-undercloud-cluster-plain-stack
  #   namespace: default
